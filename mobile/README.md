# Deep Photos Mobile

The mobile application is a Flutter app inherited from the upstream codebase.

The Dart package namespace still uses upstream names during the fork migration. Treat those names as technical compatibility details until the mobile rename is planned and tested.

## Development Notes

- Trust the local mise config before running mobile tooling.
- Add shared translations in the root `i18n` directory.
- Regenerate generated code only as part of a focused mobile/tooling task.
